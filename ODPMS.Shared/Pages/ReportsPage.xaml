<Page
    x:Class="ODPMS.Pages.ReportsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ODPMS.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <!--<Page.Resources>
        <DataTemplate x:Key="TicketListViewTemplate" x:DataType="models:Ticket">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{x:Bind Number}" x:Phase="1" Margin="0,5,0,5"/>
                <TextBlock Grid.Column="1" Text="{x:Bind Type}" x:Phase="1" Margin="0,5,0,5"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>-->
    
    <Grid>
        <ListView x:Name="TicketListView"
                  Margin="0"
                  HorizontalAlignment="Left">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Ticket Number" Margin="0,5,0,5"/>
                        <TextBlock Grid.Column="1" Text="Type" Margin="0,5,0,5"/>
                        <TextBlock Grid.Column="2" Text="Status" Margin="0,5,0,5"/>
                        <TextBlock Grid.Column="3" Text="Cost" Margin="0,5,0,5"/>
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Number}" Margin="0,5,0,5"/>
                        <TextBlock Grid.Column="1" Text="{Binding Type}" Margin="0,5,0,5"/>
                        <TextBlock Grid.Column="2" Text="{Binding Status}" Margin="0,5,0,5"/>
                        <TextBlock Grid.Column="3" Text="{Binding Cost}" Margin="0,5,0,5"/>
                    </Grid>
                </DataTemplate>

            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
