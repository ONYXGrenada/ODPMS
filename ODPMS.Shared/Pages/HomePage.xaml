<Page
    x:Class="ODPMS.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ODPMS.Pages"
    xmlns:models="using:ODPMS.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <DataTemplate x:Key="TicketListViewTemplate" x:DataType="models:Ticket">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{x:Bind Number}" x:Phase="1" Margin="0,5,0,5"/>
                <TextBlock Grid.Column="1" Text="{x:Bind Type}" x:Phase="1" Margin="0,5,0,5"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid HorizontalAlignment="Stretch"
              Padding="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="ButtonNewTicket" 
                    Grid.Column="0"
                    Content="New Ticket"
                    Style="{StaticResource AccentButtonStyle}" 
                    Click="NewTicket_Clicked"
                    HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal"
                        Grid.Column="1">
                <TextBox x:Name="TicketNumber"/>
                <Button x:Name="ButtonPayTicket"
                        Grid.Column="1"
                        Content="Pay Ticket" 
                        Style="{StaticResource AccentButtonStyle}" 
                        Click="PayTicket_Clicked"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <ListView x:Name="TicketList"
                  Grid.Row="1"
                  ItemTemplate="{StaticResource TicketListViewTemplate}"
                  Margin="0"
                  HorizontalAlignment="Left"/>
    </Grid>
</Page>
